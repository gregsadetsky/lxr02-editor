# 2026-08-08 status — lxr-02 kit editor

live at https://lxr02.greg.technology (disco project lxr02 on
my-paas-prod-aws, repo gregsadetsky/lxr02-editor, push = deploy).
25 playwright tests green.

## what got done today (big device session)
- FULL cc+nrpn map device-verified byte-for-byte (zubtest/zubflaaa a-b
  diffs, 215/215 after zeroing modulation)
- discovered + confirmed modulation writeback (running lfos/mod envs
  rewrite their target params on the device; saves capture that)
- every enum in the editor is now device-verified: click wavs (14, not
  the manual's 15), filter types, lfo waveforms (sin tri sup sdn sqr rnd
  xup xdn), lfo sync (off 4/1..1/8 12 16 32), lfo retrig (off v1-v6),
  out routing (st1 st2 l1 r1 l2 r2 fx), fm mode (0=fm 1=mix — guess was
  wrong, now fixed), dst = the param table = cc map shifted by one
  (28-value sweep), dst sliders show resolved names
- ui: device page order per voice, pan -63..+64, midi-only gating (page
  inert until output picked), statechange no longer steals the picked
  output (first send opens the port -> chrome fires statechange -> fill
  used to rebuild and drop the pick; fixed + regression test with a
  fake midi stack), status red/green, favicon, og image (diagonal
  lxr/sliders split), readme

## next steps (details in CHECKS.md)
1. b-section page walks on the device: snare/clp/hh page orders, eg rpt
   + ch/oh dec placement, and whether lfo frq / fm frq / click frq
   display scaled or verbatim
2. dst file bytes: drum1 lfo dst is parked on a known value — next card
   mount, save a kit on device and find where that value landed in the
   file (the 136+39..44 slots may belong to something else)
3. "len" on the mix page: what is it, what range (not midi-addressable)
4. ch/nte in the kit file: save same kit twice differing only in ch,
   diff locates the byte or proves absence
5. build the fx section (nrpn 106-117: type/routing/amount/dist type/
   p1-p4/ratio/ringmod wav/delay type) as a master column, then re-run
   the a2 a-b diff to verify it
6. global ch 0 mystery: in a project showing 0, test cc ch 1/5/10 all
   working (omni?) — 0 can't be dialed, only pre-existing
7. save-filename test (a3): does the device list non-"NN-" .SND files?
8. round-trip test (a4): load factory kit -> save unchanged -> diff
9. ship: video demo, open-source decision (public repo = explicit
   confirmation), post in the two threads (urls in TODO.md), maybe
   submit midi.guide corrections (cc 55/57/73/74 + all the enums)
